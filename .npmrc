# 解决 Windows 上 git 不保留文件权限的问题
# postinstall 脚本会在 npm install 后自动执行

# Electron 镜像配置（加速下载）
electron_mirror=https://npmmirror.com/mirrors/electron/
electron_builder_mirror=https://npmmirror.com/mirrors/electron-builder/
